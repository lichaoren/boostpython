#ifndef VOLUMEMATH_HPP
#define VOLUMEMATH_HPP

namespace algorithm {


    /**
     * @brief Mathematic operations between Volumes.
     * 
     * 
     * @note This feature works with interactive and G2 torpy module. 
     * Special interfaces for torpy module 
     *        are used: @ref setFormula, @ref setInputVolumes, @ref setOutputPath, @ref calculate. Although other APIs can
     *        still be used on Cluster job, the memory usage is not protected for large volumes
     * 
     * In Tornado (Horizon Picking Window: Edit->Smoothing ).
     * @image html HorizonSmooth.png
     * 
     * \code{.py}
     * \endcode
     * 
     */
//    class VolumeMath {
//    public:
//        
//    };
}

#endif /* VOLUMEMATH_HPP */

